
// Optional: requires firebase-admin and credentials if you want push
import dotenv from 'dotenv';
dotenv.config();

console.log('notifyDaily placeholder: configure Firebase to enable push notifications.');
// See guide in README.md
